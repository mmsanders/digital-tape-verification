# WP-06a — v1.1 effective writability mutators

Verifier-owned. Independent of WP-10 crash tables and of WP-36 source-slot
(`write == NULL`). Authored from Engine API §3.1 and acceptance WP-06a.

Not product acceptance. Not a merge onto verification `main`.

```sh
python3 tests/writability_draft8/selftest.py
```
