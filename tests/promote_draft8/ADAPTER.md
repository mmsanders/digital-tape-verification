# Mechanical adapter contract

`wp_promote_probe CASE_ID INPUT.vo08 OUTPUT.vo08`

Stdout one JSON object `WP-PROMOTE-OBSERVATION-1`.

Mount Side B, call `tape_promote` with a positive block budget until
`more_work == false` or a refusal, then unmount. Do not change fixtures
or expected results to fit a product build.
